apiVersion: v1
kind: Service
metadata:
  name: prescripto-service
  labels:
    app: prescripto
spec:
  selector:
    app: prescripto  
  ports:
    - port: 8000
      targetPort: 8000
  type: LoadBalancer