# apiVersion: v1
# kind: Service
# metadata:
#   name: prescripto-service
#   labels:
#     app: prescripto
# spec:
#   selector:
#     app: prescripto
#   ports:
#     - port: 80
#       targetPort: 8000
#   type: LoadBalancer
apiVersion: v1
kind: Service

metadata:
  name: prescripto-service

spec:
  type: LoadBalancer

  selector:
    app: prescripto

  ports:
  - port: 80
    targetPort: 8000